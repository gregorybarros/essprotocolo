{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Editar empresa:</h3>
<div class="card">
    <div class="card-body">
<form action="/admin/categorias/edit" method="POST">
    <input type="hidden" name="id" value="{{categorias._id}}">
    <label for="nome">Nome: </label>
        <input type="text" id="nome" name="nome" class="form-control" placeholder="{{categorias.nome}}" value="{{categorias.nome}}">
    <label for="slug">Slug: </label>
        <input type="text" id="slug" name="slug" class="form-control" placeholder="{{categorias.slug}}" value="{{categorias.slug}}">
    <button type="submit" class="btn btn-success mt-2">Editar empresa</button>
</form>
</div>
</div>
